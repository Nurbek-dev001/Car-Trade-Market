# Future Car - Автосалон


Полнофункциональная система управления автосалоном с современным веб-интерфейсом и REST API.

## 📋 Оглавление

- [Основные возможности](#-основные-возможности)
- [Технологии](#-технологии)
- [Структура проекта](#-структура-проекта)
- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Запуск](#-запуск)
- [API Документация](#-api-документация)
- [Деплой](#-деплой)
- [Тестирование](#-тестирование)
- [Лицензия](#-лицензия)

## 🚀 Основные возможности

### 🔧 Административные функции
- Управление каталогом автомобилей (CRUD операции)
- Загрузка изображений автомобилей
- Управление пользователями и ролями
- Обработка заказов и изменение статусов
- Аналитика и статистика продаж

### 👤 Пользовательские функции
- Просмотр каталога автомобилей с фильтрацией
- Расширенный поиск по параметрам
- Добавление в избранное
- Оформление заказов онлайн
- Личный кабинет пользователя
- История заказов

### 💼 Бизнес-функции
- Система управления заказами
- Интеграция с платежными системами
- Уведомления по email
- Отчеты и аналитика
- Мультиязычная поддержка (RU/KZ)

## 🛠 Технологии

### Backend
- **Node.js** + **Express** - серверная платформа
- **MongoDB** + **Mongoose** - база данных и ODM
- **JWT** - аутентификация и авторизация
- **Multer** - загрузка файлов
- **Bcrypt** - хеширование паролей
- **Express Validator** - валидация данных

### Frontend
- **React** 18 - пользовательский интерфейс
- **Material-UI** - дизайн система
- **React Router** 6 - навигация
- **React Query** - управление состоянием
- **Formik** + **Yup** - формы и валидация
- **Axios** - HTTP клиент

### DevOps
- **Docker** + **Docker Compose** - контейнеризация
- **Nginx** - реверс-прокси
- **Git** - контроль версий

## 📁 Структура проекта

```
auto-salon/
├── backend/                    # Backend приложение
│   ├── src/
│   │   ├── models/            # Модели MongoDB
│   │   │   ├── Car.js         # Модель автомобиля
│   │   │   ├── User.js        # Модель пользователя
│   │   │   └── Order.js       # Модель заказа
│   │   ├── controllers/       # Контроллеры
│   │   ├── routes/            # Маршруты API
│   │   ├── middleware/        # Промежуточное ПО
│   │   ├── seeds/            # Сидеры для тестовых данных
│   │   └── config/           # Конфигурация
│   ├── uploads/              # Загруженные изображения
│   ├── .env                  # Переменные окружения
│   └── server.js             # Точка входа сервера
├── frontend/                  # Frontend приложение
│   ├── public/               # Статические файлы
│   ├── src/
│   │   ├── components/       # React компоненты
│   │   ├── pages/           # Страницы приложения
│   │   ├── contexts/        # React контексты
│   │   ├── utils/           # Утилиты
│   │   ├── App.js           # Основной компонент
│   │   └── index.js         # Точка входа
│   ├── .env                 # Переменные окружения
│   └── package.json         # Зависимости
├── docker-compose.yml       # Docker конфигурация
├── Dockerfile.backend       # Docker образ бэкенда
├── Dockerfile.frontend      # Docker образ фронтенда
└── README.md               # Документация
```

## 🚀 Быстрый старт

### Требования
- Node.js 18 или выше
- MongoDB 6.0 или выше
- npm или yarn
- Git

### Быстрая установка (1 минута)

```bash
# Клонировать репозиторий
git clone <repository-url>
cd auto-salon

# Установить зависимости
cd backend && npm install
cd ../frontend && npm install

# Запустить MongoDB (в отдельном терминале)
mongod

# Запустить сервер (в отдельном терминале)
cd backend && npm run dev

# Запустить клиент (в отдельном терминале)
cd frontend && npm start
```

Откройте браузер и перейдите на:
- Frontend: http://localhost:3000
- Backend API: http://localhost:5000



## 🚀 Запуск

### Вариант 1: Локальная разработка

**Терминал 1 - MongoDB:**
```bash
mongod
```

**Терминал 2 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 3 - Frontend:**
```bash
cd frontend
npm start
```

### Вариант 2: Запуск с Docker

```bash
# Собрать и запустить все сервисы
docker-compose up -d --build

# Проверить статус
docker-compose ps

# Просмотреть логи
docker-compose logs -f backend

# Остановить все сервисы
docker-compose down
```

### Вариант 3: Продакшен сборка

```bash
# Backend
cd backend
npm run build
npm start

# Frontend
cd frontend
npm run build
serve -s build -l 3000
```

## 📚 API Документация

### Базовый URL
```
http://localhost:5000/api
```

### Основные эндпоинты

#### Аутентификация
```
POST   /auth/register     - Регистрация пользователя
POST   /auth/login        - Вход в систему
GET    /auth/profile      - Получение профиля
PUT    /auth/profile      - Обновление профиля
```

#### Автомобили
```
GET    /cars              - Получение списка автомобилей
GET    /cars/:id          - Получение автомобиля по ID
POST   /cars              - Создание автомобиля (admin)
PUT    /cars/:id          - Обновление автомобиля (admin)
DELETE /cars/:id          - Удаление автомобиля (admin)
GET    /cars/stats        - Статистика по автомобилям
GET    /cars/brands       - Список брендов
```

#### Заказы
```
POST   /orders            - Создание заказа
GET    /orders/my-orders  - Мои заказы
GET    /orders/:id        - Получение заказа по ID
PUT    /orders/:id/status - Обновление статуса заказа (admin)
DELETE /orders/:id/cancel - Отмена заказа
```

#### Пользователи
```
GET    /users             - Список пользователей (admin)
GET    /users/:id         - Получение пользователя (admin)
PUT    /users/:id         - Обновление пользователя (admin)
DELETE /users/:id         - Удаление пользователя (admin)
POST   /users/me/favorites- Добавить в избранное
DELETE /users/me/favorites/:carId - Удалить из избранного
```

### Примеры запросов

**Регистрация пользователя:**
```bash
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.kz",
    "password": "test123",
    "fullName": "Иван Иванов",
    "phone": "+77771234567"
  }'
```

**Получение автомобилей с фильтрацией:**
```bash
curl "http://localhost:5000/api/cars?brand=Toyota&yearFrom=2020&priceTo=20000000&limit=10"
```

## 🌐 Деплой

### Деплой на VPS (Ubuntu 20.04+)

**1. Подготовка сервера:**
```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка зависимостей
sudo apt install -y docker.io docker-compose git nginx certbot python3-certbot-nginx
sudo systemctl start docker
sudo systemctl enable docker

# Настройка firewall
sudo ufw allow 22
sudo ufw allow 80
sudo ufw allow 443
sudo ufw enable
```

**2. Клонирование проекта:**
```bash
cd /var/www
git clone <repository-url> auto-salon
cd auto-salon
```

**3. Настройка production окружения:**
```bash
# Создайте production .env файлы
nano backend/.env.production
nano frontend/.env.production
```

**4. Запуск с Docker Compose:**
```bash
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

**5. Настройка Nginx:**
```nginx
server {
    listen 80;
    server_name ваш-домен.kz www.ваш-домен.kz;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
    
    location /api {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /uploads {
        alias /var/www/auto-salon/backend/uploads;
    }
}
```

**6. SSL сертификат:**
```bash
sudo certbot --nginx -d ваш-домен.kz -d www.ваш-домен.kz
```

### Деплой на хостингах

**Heroku:**
```bash
# Backend
heroku create auto-salon-api
heroku addons:create mongolab:sandbox
git push heroku main

# Frontend
npm run build
heroku create auto-salon-web
git push heroku main
```

**Vercel (Frontend):**
```bash
npm run build
vercel --prod
```

## 🧪 Тестирование

### Unit тесты
```bash
# Backend
cd backend
npm test

# Frontend
cd frontend
npm test
```

### API тестирование (Postman)
Импортируйте коллекцию Postman из папки `docs/postman/`

### E2E тестирование
```bash
# Установка Cypress
npm install cypress --save-dev

# Запуск Cypress
npx cypress open
```

## 🐛 Устранение неполадок

### Распространенные проблемы

**1. MongoDB не запускается:**
```bash
# Проверьте статус MongoDB
sudo systemctl status mongod

# Запустите вручную
sudo mongod --dbpath /var/lib/mongodb
```

**2. Порт занят:**
```bash
# Найдите процесс, использующий порт
sudo lsof -i :5000

# Убейте процесс
sudo kill -9 <PID>
```

**3. Ошибки зависимостей:**
```bash
# Удалите node_modules и переустановите
rm -rf node_modules package-lock.json
npm install
```

**4. Docker ошибки:**
```bash
# Пересоберите образы
docker-compose down
docker-compose up -d --build

# Очистите кэш Docker
docker system prune -a
```

## 🔧 Дополнительные настройки

### Настройка email уведомлений
1. Создайте аккаунт в SendGrid или используйте SMTP
2. Обновите переменные окружения в `.env`
3. Настройте шаблоны писем в `backend/src/utils/emailTemplates.js`

### Интеграция с платежными системами
Поддерживается интеграция с:
- Kaspi Bank API
- Halyk Bank
- Stripe
- PayPal

Настройки в `backend/src/config/payment.js`

### Аналитика
Интеграция с:
- Google Analytics
- Yandex.Metrika
- Hotjar

Настройте в `frontend/src/utils/analytics.js`

## 📊 Мониторинг

### Логирование
```bash
# Просмотр логов бэкенда
docker-compose logs -f backend

# Просмотр логов фронтенда
docker-compose logs -f frontend

# Просмотр логов MongoDB
docker-compose logs -f mongodb
```

### Мониторинг производительности
- **PM2** для управления процессами Node.js
- **MongoDB Atlas** для мониторинга базы данных
- **New Relic** / **Datadog** для APM

### Резервное копирование
```bash
# Backup MongoDB
mongodump --db auto_salon --out /backup/$(date +%Y%m%d)

# Автоматический backup (cron)
0 2 * * * mongodump --db auto_salon --out /backup/$(date +\%Y\%m\%d)
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

### Правила кодирования
- Используйте Prettier для форматирования
- Следуйте ESLint конфигурации
- Пишите комментарии на русском языке
- Документируйте все публичные методы


Коммерческая поддержка:
- Телефон: +7 (777) 123-45-67
- Email: sales@auto.kz
- Telegram: @auto_support

## 📄 Лицензия

Этот проект лицензирован под MIT License - смотрите файл [LICENSE](LICENSE) для деталей.

## 👏 Благодарности

- Команде разработчиков  Future Car
- Сообществу Open Source
- Всем тестерам и пользователям

---

<div align="center">
  <sub> Лучший среди автосалонов Казахстана</sub>
</div>
